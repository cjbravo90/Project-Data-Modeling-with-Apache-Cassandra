name: CI

on: [push]

jobs:

  build:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v2
    - name: Build and run Jupyter Notebook
      run: |
        docker build -t dockerfile .
        docker docker run -p 9042:9042 -p 8888:8888 dockerfile
